const Header = () => {
  return (
    <header style={styles.header}>
      <h2>BeyondChats Article Optimizer</h2>
    </header>
  );
};

const styles = {
  header: {
    padding: "16px",
    backgroundColor: "#111827",
    color: "white",
    textAlign: "center",
  },
};

export default Header;
